<%= form_with model: @calender, local: true do |form| %>

  <div class="title">
    <%= form.label :撮影可 %>
    <%= form.check_box :group, :user_ids, {class: "check_box"}, true, false %>
  </div>

  <div class="submit">
    <%= form.submit %>
  </div>

<% end %>